/*
 * RatingBox Messages
 *
 * This contains all the text for the RatingBox container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RatingBox';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RatingBox container!',
  },
});
